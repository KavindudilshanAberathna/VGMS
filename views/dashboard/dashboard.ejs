<<<<<<< HEAD
<main class="pt-16 md:pl-60 min-h-screen">
    <div class="p-4 md:p-6 max-w-7xl mx-auto">
      <!-- Welcome Section -->
      <div class="bg-white rounded shadow-sm p-6 mb-6">
        <div class="flex flex-col md:flex-row md:items-center justify-between">
            <div>
                <h1 class="text-2xl font-semibold mb-1">Welcome, <%= user?.fullName || 'Guest' %></h1>
                <p class="text-gray-600"><%= user.role %>, VGMS</p>
                <p class="text-gray-500 text-sm mt-1" id="datetime"></p>
            </div>
            <div class="mt-4 md:mt-0">
                <button class="bg-primary text-white px-4 py-2 rounded-button flex items-center !whitespace-nowrap">
                    <i class="ri-calendar-2-line mr-2"></i>
                    <span>Book Appointment</span>
                </button>
            </div>
        </div>
    </div>
    </div>
</main>
=======
<main class="pt-16 md:pl-60 min-h-screen">
    <div class="p-4 md:p-6 max-w-7xl mx-auto">
      <!-- Welcome Section -->
      <div class="bg-white rounded shadow-sm p-6 mb-6">
        <div class="flex flex-col md:flex-row md:items-center justify-between">
            <div>
                <h1 class="text-2xl font-semibold mb-1">Welcome, <%= user?.fullName || 'Guest' %></h1>
                <p class="text-gray-600"><%= user.role %>, VGMS</p>
                <p class="text-gray-500 text-sm mt-1" id="datetime"></p>
            </div>
            <div class="mt-4 md:mt-0">
                <button class="bg-primary text-white px-4 py-2 rounded-button flex items-center !whitespace-nowrap">
                    <i class="ri-calendar-2-line mr-2"></i>
                    <span>Book Appointment</span>
                </button>
            </div>
        </div>
    </div>
    </div>
</main>
>>>>>>> 2aa45ee (Initial commit)
