<<<<<<< HEAD
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Service History</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-white text-[#800000] font-sans">

  <div class="max-w-6xl mx-auto px-4 py-10">
    <h2 class="text-4xl font-bold text-center mb-8">Service History</h2>

    <% if (appointments.length === 0) { %>
      <p class="text-center text-gray-500">No completed appointments yet.</p>
    <% } else { %>
      <div class="overflow-x-auto">
        <table class="min-w-full text-left border border-[#800000]/30 rounded-lg shadow">
          <thead class="bg-[#800000] text-white">
            <tr>
              <th class="px-4 py-3">Customer</th>
              <th class="px-4 py-3">Vehicle</th>
              <th class="px-4 py-3">Date</th>
              <th class="px-4 py-3">Time</th>
              <th class="px-4 py-3">Service</th>
              <th class="px-4 py-3">Status</th>
            </tr>
          </thead>
          <tbody>
            <% appointments.forEach(app => { %>
              <tr class="border-t hover:bg-[#800000]/5 transition">
                <td class="px-4 py-3"><%= app.customer?.fullName || 'N/A' %></td>
                <td class="px-4 py-3"><%= app.vehicleNumber %></td>
                <td class="px-4 py-3"><%= app.date.toDateString() %></td>
                <td class="px-4 py-3"><%= app.time %></td>
                <td class="px-4 py-3"><%= app.serviceType %></td>
                <td class="px-4 py-3">
                  <span class="inline-block px-2 py-1 text-sm font-medium rounded-full bg-green-100 text-green-800">
                    Completed
                  </span>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    <% } %>
  </div>

</body>
</html>
=======
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Service History</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-white text-[#800000] font-sans">

  <div class="max-w-6xl mx-auto px-4 py-10">
    <h2 class="text-4xl font-bold text-center mb-8">Service History</h2>

    <% if (appointments.length === 0) { %>
      <p class="text-center text-gray-500">No completed appointments yet.</p>
    <% } else { %>
      <div class="overflow-x-auto">
        <table class="min-w-full text-left border border-[#800000]/30 rounded-lg shadow">
          <thead class="bg-[#800000] text-white">
            <tr>
              <th class="px-4 py-3">Customer</th>
              <th class="px-4 py-3">Vehicle</th>
              <th class="px-4 py-3">Date</th>
              <th class="px-4 py-3">Time</th>
              <th class="px-4 py-3">Service</th>
              <th class="px-4 py-3">Status</th>
            </tr>
          </thead>
          <tbody>
            <% appointments.forEach(app => { %>
              <tr class="border-t hover:bg-[#800000]/5 transition">
                <td class="px-4 py-3"><%= app.customer?.fullName || 'N/A' %></td>
                <td class="px-4 py-3"><%= app.vehicleNumber %></td>
                <td class="px-4 py-3"><%= app.date.toDateString() %></td>
                <td class="px-4 py-3"><%= app.time %></td>
                <td class="px-4 py-3"><%= app.serviceType %></td>
                <td class="px-4 py-3">
                  <span class="inline-block px-2 py-1 text-sm font-medium rounded-full bg-green-100 text-green-800">
                    Completed
                  </span>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    <% } %>
  </div>

</body>
</html>
>>>>>>> 2aa45ee (Initial commit)
